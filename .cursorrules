# Tryzeon 官網 — AI Coding Rules

## 專案概述
Tryzeon 是一家 AI 時尚科技新創公司的官方網站。網站使用 Next.js 16 (App Router + Turbopack) 搭配 TypeScript、TailwindCSS、Framer Motion 開發。主要語言為繁體中文。

## 公司與產品背景

- **公司名稱**: 創然科技股份有限公司 (Tryzeon)
- **定位**: AI × 時尚科技新創，以 AI 為核心導入時尚產業，打造新世代時尚生態
- **聯絡信箱**: contact@tryzeon.com
- **社群**: Instagram @tryzeon, LinkedIn /company/tryzeon

### 五大核心產品
1. **AI 虛擬試穿** — 一張照片即可生成試穿效果
2. **AI 虛擬試穿影片** — 靜態照片轉動態影片
3. **個人雲端衣櫃 & AI 推薦** — 基於 5W1H 的穿搭建議
4. **時尚趨勢數據分析** — 幫助品牌精準進貨、優化選品決策
5. **虛擬試衣間** — 合作品牌上傳服飾，消費者挑選試穿

### 商業模式（2026-07 更新）
- **對外一律以 B2B 為主**：pay-per-use — 每 5 次試穿 NT$1，前 3 個月免費，無月費、無前期建置費
- **B2C**：消費者免費使用，是流量與數據引擎，**不是利潤中心**（App 內加購／Pro 屬輔助且尚未上線）
- **路線圖**：第 1 年 四大通路落地 + B2B 開始收費 → 第 2 年 時尚數據商品化 → 第 3 年 自有 AI 模型 API 授權
- **定位**：「別人做工具，我們做生態系」——亞洲時尚品牌 OMO 整合生態系

### 官網受眾（決定所有文案的語氣）
官網真實讀者是 **① B2B 品牌決策者 ② 科技業合作夥伴 ③ 創業競賽評審／投資人**，
**不是 B2C 消費者**。寫任何文案前先問「這句是講給誰聽的」。

### ⛔ 對外用語紅線（違反即為嚴重錯誤）
1. **禁用 84%**（無可靠來源）。61%／71%（2016 Retail Perceptions）僅可作畫面文字、不得口述來源
2. **轉換率數字必須具名 Deloitte 或 Shopify**；市場規模具名 Grand View Research
3. **禁止任何形式的退貨率宣稱**（「降低退貨率 X%」「減少退貨」皆禁）。
   消費者端只講「打破想像的隔閡」；B2B／永續脈絡可講「精準進貨降低庫存與紡織浪費」
4. **開發中功能不得寫成已上線**。已上線：雙平台 App、LINE OA(LIFF)。
   開發中：門市 QR、品牌官網 SDK、平台整合(SHOPLINE/Shopify/Cyberbiz/91APP)、三項專利方向
5. **Pre-revenue** — 絕不可出現「營收」「付費客戶」「訂閱專業版」等既成事實寫法（金流僅 sandbox）
6. **「對標 ISO 27001」，不可寫已認證**；資安用語需精確（不可寫「端對端加密」，實為 SSL/TLS + RLS）
7. **合作品牌 logo 未簽定不可放**；洽談中不可寫成已合作
8. **第三方／內部 AI 模型名不得出現**（Gemini、nano-banana、Veo3 等）；統一「團隊自家研發的專屬 AI 時尚模型」
9. **技術規格照實寫**：靜態試穿 5-7 秒、動態影片 10-15 秒。不得灌水
10. 市場宣稱邊界：可說「亞洲尚無 AI OMO 整合系統領導者」，**不可說「虛擬試穿無領導者」**

> 新增任何數據前必須確認來源可具名；無法具名就不要放。

## 技術規範

### 技術棧
- **框架**: Next.js 16 (App Router), React 18, TypeScript (strict mode)
- **樣式**: TailwindCSS 3, 禁止使用 inline CSS 或獨立 CSS 模組（除非特殊情況如 PartnerMarquee.css）
- **動畫**: Framer Motion (`framer-motion`)
- **圖示**: Lucide React (`lucide-react`)，使用 named imports
- **字型**: Google Fonts — Playfair Display (標題) + Outfit (內文)
- **路徑別名**: `@/*` 對應專案根目錄

### TypeScript 嚴格規範
- 開啟 `strict: true`、`noUnusedLocals`、`noUnusedParameters`
- 所有元件 props 必須定義 `interface`
- 禁止使用 `any` 類型，必要時使用 `unknown` 並做 type narrowing
- 使用 `as const` 建立常數物件

### 元件規範
- 所有元件使用 **named export**，不使用 default export（`export function ComponentName`）
- 客戶端元件必須在檔案頂部加 `'use client';`
- 元件檔案命名使用 PascalCase（如 `FullBleedCarousel.tsx`）
- **Code splitting 規則（區分 SEO 影響）**：
  - **含 SEO 關鍵內容的元件**（產品標題、核心數據、功能描述、FAQ 等需要被 search bot 抓取的文案）→ 必須靜態 import，確保 SSR HTML 包含完整內容
  - **純互動元件**（重型 modal、3D viewer、影片播放器、登入彈窗、編輯器、地圖等不影響 SEO 的元件）→ 使用 `dynamic()` 並設定 `{ ssr: false }` 做 code splitting
  - 不確定時優先選靜態 import，避免拆 chunk 反而傷 SEO
- 圖片一律使用 Next.js `<Image>` 元件，設定適當的 `width`、`height`、`alt`

### 程式碼風格
- 使用雙引號 `"` 作為 JSX 屬性值
- Import 順序：React → Next.js → 第三方套件 → @/ 內部模組 → 相對路徑
- 中文註解用於說明業務邏輯，英文註解用於技術說明
- 不要將所有內容放在單一檔案，超過 300 行的元件應拆分

## 品牌規範

### 品牌色彩（定義在 `lib/constants.ts`）
- 背景白: `#FFFFFF` / `#FAFAFA`
- 深米色: `#B8A094`
- 墨色（文字）: `#0B0B0B`
- 強調色: `#1E90FF`

### 設計風格
- Apple 風格的極簡設計：大量留白、精緻排版、微動畫
- 全站使用 ScrollReveal 進場動畫
- 按鈕使用 `AppleButton` / `AppleLink` 元件
- 重要區塊之間使用 `<Section>` 元件包裹
- 文字選取色: `selection:bg-[#2563EB]/15 selection:text-[#101828]`

### 品牌語調
- 科技感但不冰冷，專業但平易近人
- 主要標題使用繁體中文
- 副標描述可混用中英文
- 公司名始終寫作「Tryzeon」

## 國際化 (i18n)

- 所有使用者可見文字放在 `lib/translations.ts`
- 使用 `translations[lang]` 物件取得翻譯
- 目前支援 `zh-TW`，結構已為多語言準備
- 禁止在元件中 hardcode 使用者可見文字

## SEO 規範

- 每個頁面必須設定適當的 `metadata` (title, description, openGraph)
- 使用 JSON-LD 結構化資料（已在 `layout.tsx` 設定）
- 圖片必須有 `alt` 描述文字
- 使用語義化 HTML 標籤（`<header>`, `<main>`, `<section>`, `<footer>`）

## 效能規範

- Code splitting 依【元件規範】的 SEO 影響判定，不是「首屏 vs 非首屏」二分法
- 圖片格式優先使用 WebP/AVIF（Next.js Image 自動處理）
- 避免在元件中大量使用 `useEffect`，優先使用 CSS 動畫
- 輪播圖片使用 `priority` 標記第一張
- 行動版降階：sticky scroll cinema、複雜 parallax 等高耗能效果請以 `lg:` (≥1024px) 才啟用，行動裝置降階成靜態 stack（避免 5+ viewport 滑動 + 省電池）

## 安全規範

- 已在 `next.config.js` 設定 CSP、HSTS、X-Frame-Options 等安全 Headers
- 表單提交使用 `mode: 'no-cors'`（Google Forms）
- 禁止使用 `eval()` 或 `dangerouslySetInnerHTML`（JSON-LD 除外）

## Git 規範

- Commit message 格式: `type: 描述`
  - `feat:` 新功能
  - `fix:` 修復bug
  - `refactor:` 重構
  - `style:` 樣式調整
  - `chore:` 雜項
- **Push 前必須執行**: `git pull --rebase origin main`
- 分支: `main` 為主分支

## 資料夾結構

```
app/          → 頁面路由（App Router）
components/   → 可重用 UI 元件
lib/          → 工具函式、常數、翻譯、資料
public/       → 靜態資源（圖片）
```

## 禁止事項

1. 禁止新增 npm 套件，除非事先告知並得到批准
   - **已批准的例外**：
     - `gsap` (^3.15) — scroll-triggered cinema 用 ScrollTrigger pin/scrub，比 framer-motion useScroll 精準
     - `lenis` (^1.3) — global smooth scroll，提升整站滾動質感
   - 其他套件仍須事先批准
2. 禁止刪除已有功能，除非明確要求
3. 禁止修改 `next.config.js` 的安全 Headers 設定
4. 禁止在 `public/` 放置超過 500KB 的圖片
5. 禁止使用 `console.log`（開發除外），production 使用 `lib/analytics.ts`
6. 禁止建立與現有元件功能重複的新元件
7. **【絕對機密】禁止對外提及內部使用的 AI 模型名稱（如 nano-banana, Veo3），所有對外文案一律聲稱「團隊自家研發的專屬 AI 時尚模型」。**
